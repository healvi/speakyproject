<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.healvi.speaky">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Speaky">
        <activity android:name=".presentation.dashboard.ui.profile.langganan.LanggananActivity"></activity>
        <!-- <activity -->
        <!-- android:name=".presentation.dashboard.MainActivity" -->
        <!-- android:label="@string/title_activity_main"/> -->
        <activity android:name=".presentation.sign.SignActivity" />
        <activity android:name=".presentation.dashboard.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>