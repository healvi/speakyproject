<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.healvi.speaky">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Speaky">
        <activity android:name=".presentation.sign.SignActivity" />
        <activity android:name=".presentation.assessment.AssessmentActivity"/>
        <activity android:name=".presentation.dashboard.ui.profile.langganan.LanggananActivity" />

        <activity
            android:name=".presentation.SplashScreen"
            android:theme="@style/Theme.Speaky.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.dashboard.MainActivity" />
    </application>

</manifest>